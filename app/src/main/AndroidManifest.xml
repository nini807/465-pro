<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/home"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".loginSelect" />
        <activity android:name=".browser"/>
        <activity android:name=".imMenu" />
        <activity android:name=".customerLogin"/>
        <activity android:name=".dataAnalysis"/>
        <activity android:name=".updateStock"/>
        <activity android:name=".viewCart"/>
        <activity android:name=".wishList"/>
        <activity android:name=".smLogin"/>
        <activity android:name=".imLogin"/>
        <activity android:name=".addRemove"/>
        <activity android:name=".myAccount"/>
        <activity android:name=".smMenu"/>
        <activity android:name=".updatePrice"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>